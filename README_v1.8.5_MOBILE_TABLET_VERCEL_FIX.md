# WebInternDev v1.8.5 — Mobile/Tablet + Vercel Fix

- Final mobile/tablet viewport fitting for portrait and landscape.
- Meeting controls wrap into the available viewport instead of overflowing horizontally.
- Screen share and participant tiles are constrained to the viewport.
- Removed retired Slap and Interactive Emote UI/assets.
- Pinned stable React/Vite/LiveKit/Supabase dependency versions instead of `latest`.
- Vercel install uses `--legacy-peer-deps` for predictable deployment.
