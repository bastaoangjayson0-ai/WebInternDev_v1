# WebInternDev v1.9.1 — Centered Emote + Avatar Restore

- SHEEESHHH is centered relative to the participant tile itself.
- The default avatar image temporarily hides while the emote plays.
- The participant name remains visible.
- After the emote finishes, the default avatar automatically returns.
- Works for User, Host, and Admin.
- Emote remains button-only; no avatar hold trigger.
- Mobile/tablet sizing keeps the effect inside the participant tile.
