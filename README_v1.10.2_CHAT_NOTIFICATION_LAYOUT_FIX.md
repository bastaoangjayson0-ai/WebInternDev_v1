# WebInternDev v1.10.2 — Chat Notification + Mobile/Tablet Layout Fix

- Added a temporary in-meeting chat notification popup when another participant sends a chat message.
- Popup automatically disappears after about 3.2 seconds.
- Chat control shows an unread dot while the popup is active; opening Chat clears it.
- Preserved existing LiveKit chat behavior and chat panel.
- Hardened phone/tablet meeting layout so the meeting stays inside the viewport and the participant gallery remains the dedicated vertical scroll container.
- Phone uses 2 columns; tablet uses 3 columns.
- Prevented horizontal overflow from the page/stage.
